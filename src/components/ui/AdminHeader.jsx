import React from 'react';
import MenuTitle from "@/app/dashboard/components/MenuTitle";

function AdminHeader(props) {
 return (
  <header className={"mb-4"}>
    <h3>Welcome back Sahan</h3>
  </header>
 );}

export default AdminHeader;